# mermaid_editor

A Mermaid editor built with pure HTML, CSS, and JavaScript.

## Usage

Open `index.html` in a browser.
Type Mermaid syntax in the left panel to see the rendered diagram on the right.